import {StyleSheet} from 'react-native';

const Exstyle = StyleSheet.create({
  textinput: {
    color: 'black',
    fontSize: 30,
    borderColor: 'Blue',
    textAlign: 'center',
    padding: 10,
    borderWidth: 2,
    margin: 10,
  },
});

export default Exstyle;
